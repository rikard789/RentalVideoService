<Page
    x:Class="Frontend.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Frontend"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="DarkGray">
        <Grid Background="CornflowerBlue" Height="100" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="Wypożyczalnia filmów wideo" Margin="10" Background="Transparent" BorderBrush="Transparent" FontSize="32" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="Wypożycz DVD/kasetę" Width="200" Height="40" Margin="10" />
            <Button Grid.Column="2" Content="Wypożycz online" Width="200" Height="40" Margin="10" />
            <Button Grid.Column="3" Content="Historia wypożyczeń" Width="200" Height="40" Margin="10" />
            <Button Grid.Column="4" Content="Wyloguj się" Width="100" Height="40" Margin="10" />
        </Grid>
    </Grid>
</Page>
